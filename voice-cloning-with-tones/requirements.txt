fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
pythainlp>=5.0.0
anthropic>=0.25.0
google-genai>=0.1.0
openai>=1.0.0
python-dotenv>=1.0.1
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
python-multipart>=0.0.9

# TTS / voice cloning stack. voxcpm pulls numpy/transformers/torch transitively,
# but torch must come from the CUDA index FIRST -- plain PyPI serves a CPU-only
# build on Windows that loads fine and synthesizes far too slowly to use:
#
#   uv pip install torch==2.13.0 torchaudio==2.11.0 \
#       --index-url https://download.pytorch.org/whl/cu130
#
# cu130 (not cu128) is what carries torch 2.13; Blackwell cards (RTX 50xx,
# sm_120) need CUDA 13 kernels.
soundfile>=0.12.1
voxcpm>=2.0.3
